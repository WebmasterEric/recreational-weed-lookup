# Embed Guide

This repository provides a standalone, client-side strain lookup demo.

To embed this utility into a website or CMS:

1. Copy the HTML structure from `index.html`
2. Paste it into a page or template
3. Replace the sample strain list with your own informational links
4. Ensure all content remains informational only

No backend systems or APIs are required.